const initialState = {

}

export default function reducer (state = initialState, action) {
  switch(action.type) {
    case '':
      return{

      };
    
    case '':
      return{

      };

    default:
      return state;
  }
}